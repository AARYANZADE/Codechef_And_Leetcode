#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int t ;
cin >> t ;
while (t--){
    int n ;
    string a , b ;
    cin >> n >> a >> b ;
    if (count(a.begin(), a.end(), 'a') == count(b.begin(), b.end(), 'b')){
        cout<<"yes"<<endl;
    }
    else 
    cout<<"no"<<endl;
}
}
