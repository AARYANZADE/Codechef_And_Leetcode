#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n, m;
        cin >> n >> m;
        // 2 tyres for each bike (n) and 4 tyres for each car (m)
        cout << (2 * n + 4 * m) << "\n";
    }
    return 0;
}