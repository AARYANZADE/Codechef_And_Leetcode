#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int t;
cin >> t;
while (t--){
    int a , b , c , d ;
    cin >> a >> b >> c >> d ;
    if (d <=a + b || d <= b + c || d <= c + a){
        cout << " yes "<<endl;
    }
    else {
        cout << " no"<<endl;
    }
}
}
