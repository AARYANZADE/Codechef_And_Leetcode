#include <bits/stdc++.h>
using namespace std;

int main() {
    // Fast I/O


    int t;
    cin >> t;
    while (t--) {
        int x, y;
        cin >> x >> y;
        if (y <= x) {
            cout << y << "\n";
        } else {
            cout << x << "\n";
        }
    }

    return 0;
}